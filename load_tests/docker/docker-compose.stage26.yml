# Docker Compose Override for Stage 26 Load Test
services:
  locust-master:
    command: >
      locust -f /code/load_tests/scenarios/stage26_connection_pool.py
      --master
      --host=http://web:8000
      --users=100
      --spawn-rate=20
      --run-time=2m
      --headless
      --html=/code/reports/stage26_locust_report.html

  locust-worker:
    command: >
      locust -f /code/load_tests/scenarios/stage26_connection_pool.py
      --worker
      --master-host=locust-master
