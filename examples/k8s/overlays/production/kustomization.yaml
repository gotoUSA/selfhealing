apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - ../../base

patches:
  - path: patches/resource-limits.yaml
  - path: patches/security-context.yaml

images:
  - name: myproject
    newTag: v1.2.3  # :latest forbidden — use semantic versioning
