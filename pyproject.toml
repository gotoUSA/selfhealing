# ==========================================
# Project Metadata & Dependencies
# ==========================================
[project]
name = "myproject"
version = "0.1.0"
description = "Django REST API with Self-Healing capabilities"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [
    { name = "Admin" }
]

dependencies = [
    # ==========================================
    # Django Core
    # ==========================================
    "Django>=5.2.9",
    "asgiref>=3.8.1",
    "djangorestframework>=3.16.1",
    "django-filter>=25.2",
    "drf-nested-routers>=0.95.0",
    "drf-spectacular>=0.29.0",
    "drf-spectacular-sidecar>=2025.12.1",

    # ==========================================
    # Django Extensions
    # ==========================================
    "django-allauth>=65.13.1",
    "dj-rest-auth>=7.0.1",
    "django-celery-beat>=2.8.1",
    "django-debug-toolbar>=6.1.0",
    "django-js-asset>=3.1.2",
    "django-mptt>=0.17.0",
    "django-prometheus>=2.4.1",
    "django-redis>=6.0.0",
    "django-timezone-field>=7.1",
    "django-db-connection-pool>=1.2.6",

    # ==========================================
    # Authentication & Security
    # ==========================================
    "PyJWT>=2.10.1",
    "djangorestframework-simplejwt>=5.5.1",
    "cryptography>=46.0.3",

    # ==========================================
    # Database
    # ==========================================
    "psycopg2-binary>=2.9.11",
    "psycopg-binary>=3.3.2",
    "SQLAlchemy>=2.0.45",
    "sqlparams>=6.2.0",

    # ==========================================
    # Celery & Task Queue
    # ==========================================
    "celery>=5.5.3",
    "kombu>=5.5.4",
    "amqp>=5.3.1",
    "billiard>=4.2.2",
    "vine>=5.1.0",
    "flower>=2.0.1",
    "confluent-kafka>=2.13.0",

    # ==========================================
    # Redis
    # ==========================================
    "redis>=7.0.1",

    # ==========================================
    # HTTP & Networking
    # ==========================================
    "requests>=2.32.4",
    "httpx>=0.28.1",
    "httpcore>=1.0.9",
    "urllib3>=2.5.0",
    "certifi>=2025.11.12",
    "idna>=3.11",
    "charset-normalizer>=3.4.4",

    # ==========================================
    # OpenTelemetry (Observability)
    # ==========================================
    "opentelemetry-api>=1.39.1",
    "opentelemetry-sdk>=1.39.1",
    "opentelemetry-exporter-otlp>=1.39.1",
    "opentelemetry-exporter-otlp-proto-common>=1.39.1",
    "opentelemetry-exporter-otlp-proto-grpc>=1.39.1",
    "opentelemetry-exporter-otlp-proto-http>=1.39.1",
    "opentelemetry-instrumentation>=0.60b1",
    "opentelemetry-instrumentation-celery>=0.60b1",
    "opentelemetry-instrumentation-django>=0.60b1",
    "opentelemetry-instrumentation-logging>=0.60b1",
    "opentelemetry-instrumentation-requests>=0.60b1",
    "opentelemetry-instrumentation-wsgi>=0.60b1",
    "opentelemetry-instrumentation-psycopg2>=0.60b1",
    "opentelemetry-instrumentation-redis>=0.60b1",
    "opentelemetry-proto>=1.39.1",
    "opentelemetry-semantic-conventions>=0.60b1",
    "opentelemetry-util-http>=0.60b1",

    # ==========================================
    # OAuth
    # ==========================================
    "oauthlib>=3.2.2",

    # ==========================================
    # Metrics & Monitoring
    # ==========================================
    "prometheus_client>=0.23.1",

    # ==========================================
    # Data Serialization
    # ==========================================
    "orjson>=3.11.7",
    "PyYAML>=6.0.3",
    "msgpack>=1.1.2",

    # ==========================================
    # Validation & Schema
    # ==========================================
    "pydantic>=2.12.5",
    "pydantic-settings>=2.12.0",
    "pydantic_core>=2.41.5",
    "jsonschema>=4.25.1",
    "jsonschema-specifications>=2025.9.1",
    "referencing>=0.37.0",
    "annotated-types>=0.7.0",

    # ==========================================
    # Utilities
    # ==========================================
    "click>=8.1.7",
    "click-didyoumean>=0.3.1",
    "click-plugins>=1.1.1.2",
    "click-repl>=0.3.0",
    "colorama>=0.4.6",
    "humanize>=4.14.0",
    "python-dateutil>=2.9.0.post0",
    "python-dotenv>=1.1.1",
    "pytz>=2025.2",
    "tzdata>=2025.2",
    "six>=1.16.0",
    "tenacity>=9.1.2",
    "wrapt>=1.17.3",
    "cron_descriptor>=2.0.6",
    "python-crontab>=3.2.0,<3.3.0",
    "pillow>=12.0.0",

    # ==========================================
    # gRPC & Protobuf
    # ==========================================
    "grpcio>=1.76.0",
    "protobuf>=6.33.4",
    "googleapis-common-protos>=1.72.0",

    # ==========================================
    # etcd (Optional - Leader Election Fallback)
    # ==========================================
    "etcd3>=0.12.0",

    # ==========================================
    # LMDB (Optional - Local Cache)
    # ==========================================
    "lmdb>=1.7.5",

    # ==========================================
    # Optional Adapters (lazy import)
    # ==========================================
    "pymemcache>=4.0.0",      # Memcached adapter
    "kubernetes>=29.0.0",      # K8s deployment adapter
    "rq>=1.16.0",              # RQ queue adapter
    "rq-scheduler>=0.13.0",    # RQ scheduler
    "boto3>=1.34.0",           # AWS S3 export
    "pyarrow>=15.0.0",         # Parquet export
    "croniter>=2.0.0",         # Cron expression parsing
    "cachetools>=5.3.0",       # Precomputed cache
]

[project.optional-dependencies]
dev = [
    # ==========================================
    # Code Formatting & Linting
    # ==========================================
    "black>=24.10.0",
    "isort>=7.0.0",
    "ruff>=0.14.8",
    "mypy>=1.19.0",
    "mypy-extensions>=1.0.0",
    "autoflake>=2.3.0",
    "flake8>=7.0.0",
    "pre-commit>=3.6.0",

    # ==========================================
    # Testing
    # ==========================================
    "pytest>=9.0.1",
    "pytest-asyncio>=1.3.0",
    "pytest-celery>=1.2.1",
    "pytest-cov>=7.0.0",
    "pytest-django>=4.11.1",
    "pytest-docker-tools>=3.1.9",
    "pytest-mock>=3.15.1",
    "pytest-xdist>=3.8.0",
    "pytest-env>=1.2.0",
    "pytest-subtests>=0.11.0",
    "coverage>=7.13.0",
    "factory_boy>=3.3.3",
    "Faker>=30.8.2",
    "freezegun>=1.5.5",
    "hypothesis>=6.100.0",

    # ==========================================
    # Load Testing
    # ==========================================
    "locust>=2.42.5",
    "gevent>=25.9.1",
    "geventhttpclient>=2.3.5",
    "greenlet>=3.2.4",

    # ==========================================
    # Code Analysis
    # ==========================================
    "radon>=6.0.1",
    "vulture>=2.14",
    "pydeps>=3.0.1",
    "pipdeptree>=2.30.0",
    "import-linter>=2.9",
    "grimp>=3.14",
    "bandit>=1.9.2",
    "pygount>=3.1.0",

    # ==========================================
    # Security Analysis
    # ==========================================
    "pip-audit>=2.10.0",
    "pip-licenses>=5.5.0",
    "safety>=2.3.0",

    # ==========================================
    # Debugging
    # ==========================================
    "debugpy>=1.8.17",
    "ipdb>=0.13.13",
    "ipython>=8.20.0",
    "ipdb>=0.13.13",
    "ipython>=8.20.0",

    # ==========================================
    # WSGI Server (Production)
    # ==========================================
    "gunicorn>=21.2.0",

    # ==========================================
    # Docker
    # ==========================================
    "docker>=7.1.0",

    # ==========================================
    # FastAPI (API Testing)
    # ==========================================
    "fastapi>=0.124.2",
    "starlette>=0.50.0",

    # ==========================================
    # Flask (Flower Dashboard)
    # ==========================================
    "Flask>=3.1.2",
    "flask-cors>=6.0.1",
    "Flask-Login>=0.6.3",
    "Werkzeug>=3.1.3",
    "Jinja2>=3.1.6",
    "itsdangerous>=2.2.0",
    "blinker>=1.9.0",

    # ==========================================
    # WebSocket
    # ==========================================
    "python-socketio>=5.15.0",
    "python-engineio>=4.12.3",
    "simple-websocket>=1.1.0",
    "websocket-client>=1.9.0",
    "bidict>=0.23.1",
    "wsproto>=1.3.2",

    # ==========================================
    # SBOM & Compliance
    # ==========================================
    "cyclonedx-python-lib>=11.6.0",
    "packageurl-python>=0.17.6",
    "license-expression>=30.4.4",
    "boolean.py>=5.0",
    "py-serializable>=2.1.0",
    "sortedcontainers>=2.4.0",

    # ==========================================
    # Misc Dev Tools
    # ==========================================
    "anyio>=4.12.0",
    "attrs>=25.4.0",
    "brotli>=1.2.0",
    "CacheControl>=0.14.4",
    "ConfigArgParse>=1.7.1",
    "defusedxml>=0.7.1",
    "execnet>=2.1.2",
    "filelock>=3.20.1",
    "h11>=0.16.0",
    "inflection>=0.5.1",
    "iniconfig>=2.3.0",
    "librt>=0.7.3",
    "mando>=0.7.1",
    "markdown-it-py>=4.0.0",
    "MarkupSafe>=3.0.3",
    "mdurl>=0.1.2",
    "mpmath>=1.3.0",
    "packaging>=25.0",
    "pathspec>=0.12.1",
    "pip-api>=0.0.34",
    "pip-requirements-parser>=32.0.1",
    "platformdirs>=4.3.6",
    "pluggy>=1.6.0",
    "prettytable>=3.17.0",
    "prompt_toolkit>=3.0.52",
    "psutil>=7.1.3",
    "pycparser>=2.23",
    "Pygments>=2.19.2",
    "pyparsing>=3.2.5",
    "rich>=14.2.0",
    "rpds-py>=0.29.0",
    "sqlparse>=0.5.1",
    "stdlib-list>=0.12.0",
    "sympy>=1.13.3",
    "tomli>=2.3.0",
    "tomli_w>=1.2.0",
    "tornado>=6.5.2",
    "typing_extensions>=4.15.0",
    "typing-inspection>=0.4.2",
    "uritemplate>=4.2.0",
    "wcwidth>=0.2.14",
    "zipp>=3.23.0",
    "zope.event>=6.1",
    "zope.interface>=8.1.1",
    "importlib_metadata>=8.7.1",
    "cffi>=2.0.0",
    "annotated-doc>=0.0.4",

    # ==========================================
    # API Schema Testing (Schemathesis)
    # ==========================================
    "schemathesis>=3.25.0",
    "hypothesis-jsonschema>=0.23.0",
    "hypothesis-graphql>=0.11.0",

    # ==========================================
    # Git & Version Control
    # ==========================================
    "GitPython>=3.1.40",

    # ==========================================
    # Authentication (OAuth/OIDC)
    # ==========================================
    "Authlib>=1.6.0",

    # ==========================================
    # Mutation Testing (Optional)
    # ==========================================
    "mutmut>=2.4.0",

    # ==========================================
    # CLI Tools
    # ==========================================
    "typer>=0.9.0",
    "textual>=0.50.0",

    # ==========================================
    # NLP (Optional - for text analysis)
    # ==========================================
    "nltk>=3.8.0",

    # ==========================================
    # YAML Processing
    # ==========================================
    "ruamel.yaml>=0.18.0",

    # ==========================================
    # Misc Venv-only packages
    # ==========================================
    "drf-yasg>=1.21.0",
    "marshmallow>=3.21.0",
    "libcst>=1.1.0",
    "virtualenv>=20.25.0",
    "nodeenv>=1.8.0",
    "identify>=2.5.0",
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["shopping*", "myproject*"]
exclude = ["tests*", "htmlcov*", "staticfiles*", "media*"]

# ==========================================
# Black Configuration (Code Formatting)
# ==========================================
[tool.black]
line-length = 127
target-version = ['py312']
exclude = '''
/(
    \.git
  | \.venv
  | venv
  | env
  | __pycache__
  | migrations
  | staticfiles
  | media
  | htmlcov
  | \.backup
)/
'''

# ==========================================
# isort Configuration (Import Sorting)
# ==========================================
[tool.isort]
profile = "black"
line_length = 127
skip_glob = ["*/migrations/*", "venv/*", "env/*", "htmlcov/*"]
known_django = ["django"]
known_third_party = [
    "rest_framework",
    "celery",
    "redis",
    "mptt",
    "allauth",
    "dj_rest_auth",
    "drf_spectacular"
]
known_first_party = ["shopping", "myproject"]
sections = ["FUTURE", "STDLIB", "DJANGO", "THIRDPARTY", "FIRSTPARTY", "LOCALFOLDER"]

# ==========================================
# pytest Configuration
# ==========================================
[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "myproject.settings.test"
python_files = ["tests.py", "test_*.py", "*_tests.py"]
python_classes = ["Test*", "*Tests", "*TestCase"]
python_functions = ["test_*"]
addopts = """
    -n auto
    --dist=loadscope
    --cov=packages/selfhealing-python/src/selfhealing
    --cov-report=html
    --cov-report=term-missing:skip-covered
    --cov-fail-under=70
    --tb=short
    --strict-markers
    -m 'not load_test and not manual and not concurrency and not performance and not auth_race and not payment_race and not order_race and not point_race and not return_race and not heavy and not slow and not e2e and not requires_redis and not requires_db and not flaky'
"""
# Selfhealing 패키지 Coverage 측정:
# 1. 통합 테스트 (Docker 환경 필요): shopping/tests, tests 에서 실행
# 2. 패키지 유닛 테스트 (로컬 실행 가능):
#    cd packages/selfhealing-python && python -m pytest tests --cov=src/selfhealing --cov-report=term-missing
testpaths = ["shopping/tests", "tests"]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "unit: marks tests as unit tests (fast, no external deps)",
    "integration: marks tests as integration tests",
    "e2e: marks tests as end-to-end tests (requires full infrastructure)",
    "performance: marks tests as performance tests",
    "load_test: load test related (excluded by default in CI)",
    "manual: manual/debugging tests (excluded by default in CI)",
    "concurrency: concurrency tests (race condition verification)",
    "auth_race: authentication related race condition tests",
    "payment_race: payment related race condition tests",
    "order_race: order related race condition tests",
    "point_race: point related race condition tests",
    "return_race: return related race condition tests",
    "heavy: high-load stress tests (optionally excluded in CI)",
    "schema: OpenAPI schema-based contract tests (Schemathesis)",
    "stateful: Stateful API workflow tests (state transition verification)",
    "fuzz: Fuzz tests - Hypothesis/Schemathesis based random input verification",
    "negative: Negative tests - proper error handling for invalid inputs",
    "security: Security tests - SQL Injection, XSS, SSTI attack defense verification",
    "chaos: Chaos engineering tests - fault injection and recovery verification",
    "compound: Compound failure tests - multiple failure modes simultaneously",
    # Self-Healing Test Tiers (Reference: docs/testing/SELF_HEALING_TEST_MATRICES.md)
    "tier1: Fast unit tests for PR validation (< 2 min, no external deps)",
    "tier2: Integration tests for merge to main (< 10 min, requires DB/Redis)",
    "tier3_chaos: Chaos engineering tests for nightly runs (< 30 min)",
    "tier4_load: Load and stress tests for releases (< 2 hours, dedicated runner)",
    # Self-Healing Domain Markers
    "tenant_aware: Multi-tenancy isolation tests (R-001)",
    "cost_sensitive: Cost-aware recovery tests (R-002)",
    "requires_redis: Tests requiring Redis connection",
    "requires_celery: Tests requiring Celery worker",
    "requires_db: Tests requiring database connection",
    "flaky: Tests with known flakiness (quarantined from CI)",
]
filterwarnings = [
    # Ignore dj-rest-auth deprecation warnings (library internal, cannot fix)
    "ignore:app_settings.USERNAME_REQUIRED is deprecated:UserWarning:dj_rest_auth.registration.serializers",
    "ignore:app_settings.EMAIL_REQUIRED is deprecated:UserWarning:dj_rest_auth.registration.serializers",
    # Ignore factory_boy deprecation warning (library internal)
    "ignore:.*_after_postgeneration will stop saving the instance.*:DeprecationWarning:factory.django",
    # Ignore coverage warnings from pytest-xdist parallel execution
    "ignore:No data was collected:coverage.exceptions.CoverageWarning",
    "ignore::coverage.exceptions.CoverageWarning",
    # Ignore CovReportWarning from pytest-cov
    "ignore::pytest_cov.plugin.CovReportWarning",
]

# pytest-env: Set environment variables before test execution
# Resolves PostgreSQL cp949 encoding issue on Windows
[tool.pytest_env]
PGCLIENTENCODING = "UTF8"
PYTHONIOENCODING = "utf-8"

# ==========================================
# Selfhealing Coverage 측정 가이드
# ==========================================
# 1. Selfhealing 패키지 유닛 테스트 (로컬 실행, Docker 불필요):
#    cd packages/selfhealing-python
#    python -m pytest tests --cov=src/selfhealing --cov-report=term-missing
#
# 2. Django 통합 테스트 (Docker 환경 필요):
#    docker-compose up -d && python -m pytest tests/self_healing
#
# 3. 전체 테스트 (shopping 테스트베드 포함):
#    Root에서: python -m pytest (shopping/tests + tests 실행)
# ==========================================
# coverage Configuration
# ==========================================
[tool.coverage.run]
source = ["packages/selfhealing-python/src/selfhealing"]
omit = [
    "*/migrations/*",
    "*/tests/*",
    "*/test_*.py",
    "*/__pycache__/*",
    "*/venv/*",
    "*/env/*",
    "manage.py",
    "myproject/wsgi.py",
    "myproject/asgi.py",
    "load_tests/*",
]
branch = true
# Parallel testing support (pytest-xdist)
parallel = true
concurrency = ["thread", "multiprocessing"]
# Explicitly set data file path
data_file = ".coverage"
# Do not retain existing data during parallel execution (measure fresh each time)
relative_files = true

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "def __str__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
]
precision = 2
show_missing = true
skip_covered = false

[tool.coverage.html]
directory = "htmlcov"

# ==========================================
# radon Configuration (Code Complexity)
# ==========================================
[tool.radon]
exclude = "*/migrations/*,*/tests/*,*/__pycache__/*,*/venv/*"
cc_min = "C"  # Show complexity C or above (A=1-5, B=6-10, C=11-20, D=21-30, E=31-40, F=41+)

# ==========================================
# bandit Configuration (Security Analysis)
# ==========================================
[tool.bandit]
exclude_dirs = ["tests", "migrations", "venv", "env", "htmlcov", "load_tests"]
skips = ["B101", "B311"]  # B101: assert usage, B311: random (non-security use)
targets = ["packages/selfhealing-python/src/selfhealing"]

# ==========================================
# vulture Configuration (Dead Code Detection)
# ==========================================
[tool.vulture]
exclude = ["*/migrations/*", "*/tests/*", "venv/", "env/", "htmlcov/"]
min_confidence = 80
paths = ["shopping"]
sort_by_size = true

# ==========================================
# mutmut Configuration (Mutation Testing)
# ==========================================
# Not yet introduced in this project
# Will be introduced after core logic stabilization
#
# mutmut supports setup.cfg or [tool.mutmut] in pyproject.toml
# Limited to money-related services only
#
# [tool.mutmut]
# paths_to_mutate = [
#     "shopping/services/payment_service.py",
#     "shopping/services/point_service.py",
#     "shopping/services/point_query_service.py",
#     "shopping/services/cart_service.py",
#     "shopping/services/order_service.py",
#     "shopping/services/return_service.py",
#     "shopping/services/toss_webhook_service.py",
# ]
# tests_dir = "shopping/tests/"
# runner = "python -m pytest -x --tb=short -q"
# dict_synonyms = "Struct,NamedStruct"

# ==========================================
# deptry Configuration (Unused Dependency Detection)
# ==========================================
# 사용: deptry .
# DEP001: 누락된 의존성, DEP002: 미사용 의존성, DEP003: 전이 의존성
[tool.deptry]
extend_exclude = [
    "load_tests",
    "tests",
    "htmlcov",
    "staticfiles",
    "media",
    "migrations",
]
# 내부/테스트 모듈 - DEP001 무시
ignore_missing = [
    "integrity",           # 내부 모듈 (상대 import)
    "wal",                 # 내부 모듈 (상대 import)
    "backports",           # Python 3.9+ 호환용 (3.12에서 불필요)
    "nonexistent_module",  # 테스트용 의도적 import
]

# CLI 도구 (import 안 함) - 오탐 무시
[tool.deptry.per_rule_ignores]
DEP002 = [
    # === CLI 도구 (터미널에서만 사용) ===
    "black", "isort", "ruff", "mypy", "autoflake", "flake8", "pre-commit",
    "pytest", "pytest-asyncio", "pytest-celery", "pytest-cov", "pytest-django",
    "pytest-docker-tools", "pytest-mock", "pytest-xdist", "pytest-env", "pytest-subtests",
    "coverage", "locust", "radon", "vulture", "pydeps", "pipdeptree",
    "import-linter", "bandit", "pygount", "pip-audit", "pip-licenses", "safety",
    "debugpy", "ipdb", "ipython", "gunicorn", "mutmut", "deptry",

    # === 다른 패키지의 런타임 의존성 ===
    "kombu", "amqp", "billiard", "vine",           # Celery 의존성
    "asgiref", "sqlparse",                          # Django 의존성
    "Werkzeug", "Jinja2", "itsdangerous", "blinker", "flask-cors", "Flask-Login",  # Flask/Flower 의존성
    "click", "click-didyoumean", "click-plugins", "click-repl", "colorama",        # CLI 의존성
    "MarkupSafe", "Pygments", "rich", "prompt_toolkit", "wcwidth",                 # 터미널 UI 의존성
    "grpcio", "protobuf", "googleapis-common-protos",                              # OpenTelemetry gRPC 의존성
    "pydantic_core", "annotated-types", "typing-inspection",                       # Pydantic 의존성
    "anyio", "h11", "httpcore",                                                    # HTTP 의존성
    "cffi", "pycparser",                                                           # 암호화 의존성
    "zope.event", "zope.interface", "greenlet", "gevent", "geventhttpclient",      # 비동기 의존성
    "packaging", "platformdirs", "filelock", "pluggy", "iniconfig", "execnet",     # 유틸리티 의존성
    "attrs", "tomli", "tomli_w", "pathspec",                                       # 파싱 의존성
    "rpds-py", "referencing", "jsonschema-specifications",                         # JSON Schema 의존성
    "importlib_metadata", "zipp", "typing_extensions",                             # Python 버전 호환 의존성
    "virtualenv", "nodeenv", "identify",                                           # 환경 관리 의존성

    # === 선택적/환경별 의존성 ===
    "opentelemetry-instrumentation-psycopg2", "opentelemetry-instrumentation-redis",  # DB/Cache 계측
    "django-prometheus", "django-celery-beat", "django-timezone-field",               # Django 확장
    "django-js-asset", "drf-spectacular-sidecar",                                     # 정적 자산
    "flower", "httpx", "certifi", "idna", "charset-normalizer",                       # HTTP 클라이언트
    "psycopg-binary", "sqlparams",                                                    # DB 드라이버
    "msgpack", "jsonschema",                                                          # 직렬화
    "humanize", "python-dateutil", "pytz", "tzdata", "six", "wrapt",                  # 유틸리티
    "cron_descriptor", "python-crontab", "pillow",                                    # 스케줄/이미지
    "tornado", "uritemplate", "pyparsing", "prettytable",                             # 기타 유틸리티
    "stdlib-list", "sympy", "mpmath",                                                 # 수학/표준 라이브러리
    "CacheControl", "ConfigArgParse", "defusedxml", "brotli",                         # HTTP/XML/압축
    "markdown-it-py", "mdurl", "librt", "mando",                                      # 문서/CLI
    "pip-api", "pip-requirements-parser", "annotated-doc",                            # pip 도구
    "python-socketio", "python-engineio", "simple-websocket", "websocket-client", "bidict", "wsproto",  # WebSocket
    "cyclonedx-python-lib", "packageurl-python", "license-expression", "boolean.py", "py-serializable", "sortedcontainers",  # SBOM

    # === 테스트/개발 도구 ===
    "fastapi", "starlette", "Faker",                                                  # 테스트 서버/데이터
    "schemathesis", "hypothesis", "hypothesis-jsonschema", "hypothesis-graphql",      # 속성 기반 테스트
    "GitPython", "Authlib", "oauthlib",                                               # Git/OAuth
    "typer", "textual", "nltk", "ruamel.yaml",                                        # 추가 도구
    "drf-yasg", "marshmallow", "libcst",                                              # API/직렬화/코드 변환
    "urllib3", "mypy-extensions", "grimp", "docker", "Flask", "inflection",           # 추가 런타임/도구 의존성
]
